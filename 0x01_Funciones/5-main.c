#include "main.h"
#include <stdio.h>

// divide dos numeros

int main (void)

{
    int resultado=0;
    float divi (int a, int b);
    
    resultado = divi(12,10);
    printf ("El resultado de la division es: %d \n", resultado);
    resultado= divi (8,2);
    printf ("El resultado de la division es: %d \n", resultado);
    resultado = divi (16,8);
    printf ("El resultado de la division es: %d \n", resultado);

    return 0;
}

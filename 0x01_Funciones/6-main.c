#include "main.h"
#include <stdio.h>

// calcula el modulo de un numero

int main (void)
{
    int resultado =0;
    float modu(int a, int b);

    resultado =modu (12,10);
    printf ("El modulo es: %d \n", resultado);
    resultado = modu (87,23);
    printf ("El modulo es: %d \n", resultado);
    resultado = modu(93, 3);
    printf("El modulo es: %d \n", resultado);
    
    return 0;
}
